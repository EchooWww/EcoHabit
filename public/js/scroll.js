// function scroll() {
//     document.scrollingElement.scroll(1, 1);
// };

// scroll();

const chatMessages = document.getElementById('scroller');
chatMessages.scrollTop = chatMessages.scrollHeight;
